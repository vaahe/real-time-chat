syntax = "proto3";

package auth;

service AuthService {
    rpc Test (TestRequest) returns (TestResponse);
}

message TestRequest {
    string message = 1;
}

message TestResponse {
    string reply = 1;
}