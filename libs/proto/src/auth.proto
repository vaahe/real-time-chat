syntax = "proto3";

package auth;

service AuthService {
    rpc Test (TestRequest) returns (TestResponse);
}

message TestRequest {
    string name = 1;
}

message TestResponse {
    string message = 1;
}